import { Metadata } from 'next';
import { getAllArticles } from '@/lib/articles';
import ArticleCard from '@/components/ArticleCard';

export const metadata: Metadata = {
  title: 'Threat Intelligence - Cyber Threat Analysis',
  description: 'Advanced threat intelligence, analysis, and cybersecurity research.',
};

export default function ThreatIntelligencePage() {
  const articles = getAllArticles();

  return (
    <div className="min-h-screen pt-24 pb-16">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="mb-12">
          <h1 className="text-4xl md:text-5xl font-display font-bold text-white mb-4">
            Threat <span className="text-purple-400">Intelligence</span>
          </h1>
          <p className="text-lg text-gray-400">
            Advanced threat analysis and intelligence reports.
          </p>
        </div>

        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          {articles.map((article) => (
            <ArticleCard key={article.slug} article={article} />
          ))}
        </div>
      </div>
    </div>
  );
}
